#include<stdio.h>

int sum(int x, int y)
{   
    int sum = x+y;    
    return sum;
}
int main()
{
    printf("Your output: %d\n", sum(10,20));
    printf("Your output: %d", sum(20,20));
    return 0;
}
